import {
  register,
  login,
  logout,
  emailToken,
  forgotPassword,
  forgotPasswordToken,
  resetPassword,
  updateMe,
  follow
} from './validates';

const validate = {
  register,
  login,
  logout,
  emailToken,
  forgotPassword,
  forgotPasswordToken,
  resetPassword,
  updateMe,
  follow
};

export default validate;
