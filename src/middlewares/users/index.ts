import {
  register,
  login,
  logout,
  emailToken,
  forgotPassword,
  forgotPasswordToken,
  resetPassword
} from './validates';

const validate = {
  register,
  login,
  logout,
  emailToken,
  forgotPassword,
  forgotPasswordToken,
  resetPassword
};

export default validate;
