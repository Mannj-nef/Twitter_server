import {
  register,
  login,
  logout,
  emailToken,
  forgotPassword,
  forgotPasswordToken,
  resetPassword,
  updateMe
} from './validates';

const validate = {
  register,
  login,
  logout,
  emailToken,
  forgotPassword,
  forgotPasswordToken,
  resetPassword,
  updateMe
};

export default validate;
