import {
  register,
  login,
  logout,
  emailToken,
  forgotPassword,
  forgotPasswordToken,
  resetPassword,
  updateMe,
  follow,
  changePassword
} from './validates';

const validate = {
  register,
  login,
  logout,
  emailToken,
  forgotPassword,
  forgotPasswordToken,
  resetPassword,
  updateMe,
  follow,
  changePassword
};

export default validate;
